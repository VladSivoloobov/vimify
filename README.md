# 🧠 **Vimify — Современная LSP-конфигурация для Vim/Neovim**

> Минималистичная, но мощная настройка с поддержкой `coc.nvim`, автодополнения, диагностики, навигации, рефакторинга и встроенного терминала.  
> Идеально подходит для TypeScript, JavaScript, Rust, Python и full-stack разработки.

---

## 📦 Требования

- **Vim 8.2+** или **Neovim 0.7+**
- `npm` (для установки LSP-серверов)
- (опционально) `ripgrep` — для `:Rg` в fzf

---

## 🛠️ Установка

1. Установите [vim-plug](https://github.com/junegunn/vim-plug):
   ```bash
   curl -fLo ~/.vim/autoload/plug.vim --create-dirs \
       https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim
   ```

2. Скопируйте содержимое `init.vim` в:
   - Vim: `~/.vimrc`
   - Neovim: `~/.config/nvim/init.vim`

3. Запустите редактор и выполните:
   ```vim
   :PlugInstall
   ```

4. Установите LSP-серверы через `coc.nvim`:
   ```vim
   :CocInstall coc-tsserver coc-eslint coc-json coc-prettier
   ```

---

## 🔧 Основные настройки из `coc-example-config.vim`

### Базовая конфигурация
```vim
set encoding=utf-8
set nobackup
set nowritebackup
set updatetime=300
set signcolumn=yes
```

> Эти параметры улучшают стабильность LSP и предотвращают конфликты с резервными файлами.

---

## ⌨️ Горячие клавиши (`coc.nvim`)

### 🔍 Навигация и диагностика
| Клавиша | Действие |
|--------|--------|
| `[g` / `]g` | Перейти к предыдущей/следующей ошибке |
| `gd` | Перейти к определению |
| `gy` | Перейти к определению типа |
| `gi` | Перейти к реализации |
| `gr` | Найти все ссылки |
| `K` | Показать документацию (hover) |

### 🛠️ Рефакторинг
| Клавиша | Действие |
|--------|--------|
| `<leader>rn` | Переименовать символ |
| `<leader>f` | Форматировать выделенное |
| `<leader>a` | Code Action (выделенное) |
| `<leader>ac` | Code Action (под курсором) |
| `<leader>as` | Code Action (весь файл) |
| `<leader>qf` | Быстрое исправление ошибки |
| `<leader>re` / `<leader>r` | Рефакторинг |
| `<leader>cl` | Выполнить CodeLens |

### 📋 Управление через CoCList
| Клавиша | Действие |
|--------|--------|
| `<space>a` | Все диагнозы (`:CocList diagnostics`) |
| `<space>e` | Управление расширениями |
| `<space>c` | Список команд |
| `<space>o` | Outline текущего файла |
| `<space>s` | Поиск символов по проекту |
| `<space>j` / `<space>k` | Следующий/предыдущий в списке |
| `<space>p` | Возобновить последний список |

### 🧩 Прочие команды
```vim
:Format        " Форматировать буфер
:OR            " Организовать импорты
:Fold          " Свернуть код
```

### 📏 Выделение и прокрутка
- `<C-s>` — расширить выделение до семантического диапазона
- `<C-f>` / `<C-b>` — прокрутка float-окон (документация, попапы)

---

## 💻 Встроенный терминал (`toggleterm.nvim`)

> Не входит в `coc-example-config.vim`, но критически важен для современной разработки.

### Установка
Добавьте в секцию плагинов:
```vim
Plug 'akinsho/toggleterm.nvim', {'tag': '*'}
```

### Настройка
```vim
lua << EOF
require("toggleterm").setup({
  direction = "float",
  open_mapping = [[<c-t>]],
  insert_mappings = true,
  terminal_mappings = true,
  start_in_insert = true
})
EOF
```

### Горячие клавиши
| Клавиша | Действие |
|--------|--------|
| `<C-t>` | Открыть/закрыть float-терминал |
| `1<C-t>`, `2<C-t>` | Открыть терминал с ID 1, 2... |

> Внутри терминала:
> - `Esc` → выйти в normal mode
> - `<C-h/j/k/l>` → навигация между окнами (настройте отдельно)

---

## ♻️ Автоматическое управление сессиями

Добавьте в конец конфига:
```vim
autocmd VimLeavePre * silent! mksession! ~/.vim_session.vim
autocmd VimEnter * nested if argc() == 0 && filereadable(expand('~/.vim_session.vim'))
  \ | source ~/.vim_session.vim | endif
```

> При запуске без аргументов (`nvim`) — восстанавливает предыдущую сессию:  
> открытые файлы, позиции курсора, макросы, история.

---

## 🎨 Интеграция со статусной строкой

```vim
set statusline^=%{coc#status()}%{get(b:,'coc_current_function','')}
```

> Поддерживается в `vim-airline`, `lightline.vim` и нативной строке.

---

## ✅ Рекомендуемые плагины (дополнительно)

| Плагин | Назначение |
|-------|----------|
| `fzf.vim` | Быстрый поиск по файлам и коду |
| `NERDTree` | Файловый менеджер |
| `noice.nvim` | Современные уведомления |
| `which-key.nvim` | Подсказки по префиксам (`<leader>`) |

---

## 💡 Советы

- Всегда проверяйте, не конфликтует ли `<Tab>` с другими плагинами:  
  ```vim
  :verbose imap <Tab>
  ```
- Для TypeScript/JS используйте `coc-tsserver` — он включает навигацию, автоимпорты, организацию импортов.
- Используйте `<leader>rn` для **безопасного переименования** — это работает на уровне LSP, а не текста.

---

> **Vimify** — это не просто конфиг, а основа для профессиональной, быстрой и интеллектуальной среды разработки в Vim/Neovim.
